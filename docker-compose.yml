version: '3'

services:
  scout:
    tty: true
    ports:
      - "8000:8000"
    command: uvicorn main:app --reload --app-dir backend/app --host 0.0.0.0
    build:
      dockerfile: backend/Dockerfile
      context: .